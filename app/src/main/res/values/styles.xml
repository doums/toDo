<resources>

    <!-- Main activity theme -->
    <style name="MainActivityTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionModeOverlay">true</item>
        <item name="colorPrimary">@color/orangePrimary</item>
        <item name="colorPrimaryDark">@color/orangePrimaryDark</item>
        <item name="colorAccent">@color/orangeLight</item>
    </style>

    <!-- Main activity background -->
    <style name="MainActivityBg">
        <item name="android:background">@color/orangeLight</item>
    </style>

    <!-- AddTask activity theme -->
    <style name="AddTaskActivityTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionModeOverlay">true</item>
        <item name="colorPrimary">@color/brownPrimary</item>
        <item name="colorPrimaryDark">@color/brownPrimaryDark</item>
        <item name="colorAccent">@color/brownLight</item>
    </style>

    <!-- Toolbar -->
    <style name="AppTheme.ActionBar" parent="Widget.AppCompat.ActionBar" />
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat" />

    <!-- The EditText for add task -->
    <style name="add_task_EditText">
        <item name="android:background">@null</item>
        <item name="android:padding">10dp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/blackHint</item>
    </style>

    <!-- AddTask activity background -->
    <style name="AddTaskActivityBg">
        <item name="android:background">@color/greyBg</item>
    </style>

</resources>
